ingress:
  enabled: false
  host: kblocks.io
  tls:
    secret: kblocks-tls

backend:
  secretName: kblocks-api-secrets

redis:
  enabled: false

localCluster:
  enabled: false

secrets:
  kblocks:
    enabled: true

  portal:
    enabled: true

  gallery:
    enabled: false
