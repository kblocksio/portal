{
  "name": "autobuild",
  "version": "1.0.0",
  "main": "index.js",
  "description": "Builds and publishes a container image for a GitHub repository and optionally updates a Kubernetes resource with the new image tag.",
  "dependencies": {
    "cdktf": "^0.20.9",
    "@cdktf/provider-github": "^14.3.1"
  }
}
