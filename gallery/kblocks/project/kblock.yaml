apiVersion: kblocks.io/v1
kind: Block
spec:
  engine: noop
  definition:
    readme: ./README.md
    schema: ./src/values.schema.json
    description: A project is a collection is resources
    group: kblocks.io
    version: v1
    kind: Project
    categories:
      - environments
    plural: projects
    icon: FolderOpenDot
    singular: project
metadata:
  name: projects.kblocks.io
