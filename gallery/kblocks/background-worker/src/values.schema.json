{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://example.com/cron-schema.json",
    "$defs": {
      "commonDefs": {
        "$ref": "../../common.values.schema.json"
      }
    },
    "type": "object",
    "required": [
      "region",
      "instanceType",
      "image"
    ],
    "properties": {
      "image": {
        "description": "The Docker image or GitHub repository to use for the background worker.\n@ui kblocks.io/one-of",
        "$ref": "#/$defs/commonDefs/source"
      },
      "environmentVariables": {
        "$ref": "#/$defs/commonDefs/environmentVariables"
      },
      "instanceType": {
        "$ref": "#/$defs/commonDefs/instanceType",
      },
      "region": {
        "$ref": "#/$defs/commonDefs/region",
        "description": "The region to deploy the background worker. Your services in the same [region](https://kblocks.io/docs/regions) can communicate over a [private network](https://kblocks.io/docs/private-network).",
      }
    }
  }