apiVersion: kblocks.io/v1
kind: Block
spec:
  engine: noop
  definition:
    readme: ./README.md
    schema: ./src/values.schema.json
    description: An example of a noop block
    group: core
    version: v1
    kind: Service
    plural: services
    singular: service
metadata:
  name: services.core
